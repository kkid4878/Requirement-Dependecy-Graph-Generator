
void build_graph()
{
    //reads trhough the nodes and their connections to produce a data tree
    // data tree should resemble the flow of the full program
    //calls draw graph
};

void draw_graph(char* datatree)
{
    //takes the graph from build graph and draws it via some kind of output
    //pointer should be to a data structure but not sure what I want it to be yet
};