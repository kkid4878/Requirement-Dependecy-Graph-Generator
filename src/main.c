#include <stdio.h>
#include "file-load-parse.h"

int main() {
    
    //ask user for file / find file
    //then load and parse it

     char filename[256];
    load_file(filename);
    parse_file();

    //load
    
    

}
